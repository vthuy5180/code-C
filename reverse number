#include <stdio.h>
#include <conio.h>
int main()
{
    int n,a,b,c,ld,m;
    printf("Nhap mot so 3 chu so = ");
    scanf("%3d",&n);
    a = int(n/100);
    ld = n % 100;
    b = int(ld/10);
    c = ld%10;
    m = c*100+b*10+a;
    printf("So dao nguoc = %d",m);
    return 0;
}
